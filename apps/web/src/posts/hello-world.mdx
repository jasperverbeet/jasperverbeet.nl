---
title: Consistent Navigation Across My Inconsistent Websites
date: '2023-04-26'
description: Welcome to my first blog.
---

import Profile from '@/public/profile.jpeg'

This is my **first** blog post using *markdown*. Here’s an example of some icons that transitioned well in the Big Sur-ification of macOS icons:

# My subheading

Here is an image:

![This is me](/profile.jpeg)

I wrote  a blog about me. [This is me](google.com). Looks pretty, right?

There is a code snippet below:

```tsx
import Tooltip from "3rd-party-tooltip-on-npm";

function UserAvatar({ src, name }) {
  const [showTooltip, setShowTooltip] = useState(false);
  return (
    <div>
      <img
        src={src}
        alt={`Profile photo of ${name}`}
        width="32"
        height="32"
      />
      {showTooltip && <Tooltip>{name}</Tooltip>}
    </div>
  )
}

// const a = 123;
// const b = 321;
```

This is a list

- Item 1
- Item 2
- Item 3
- Item 4

This is inline code `const a = 123;{:javascript}`